# Core ingestion stack (Milestones 1–4)
SQLAlchemy>=2.0,<2.1
psycopg[binary]>=3.1
pydantic>=2.5
pydantic-settings>=2.1
pandas>=2.0
requests>=2.31
tenacity>=8.2
python-dotenv>=1.0
pytest>=7.4

# OSINT collector + parser (Milestone 3 — local, no external LLM)
# Telethon: one-time historical Telegram scrape → frozen data/osint/osint_flags.csv (NOT a cron/daemon).
telethon>=1.34
# Parsing uses local Ollama over its HTTP API via `requests` (no external LLM provider).
# langgraph>=0.2          # optional: wrap the Ollama parse node in a graph
# langchain-ollama>=0.2   # optional: ChatOllama wrapper (we call Ollama HTTP directly to stay light)
APScheduler>=3.10

# Stage-2 analytical export (read-only Postgres → Parquet bundle; out-of-DB ASOF join)
duckdb>=1.0
pyarrow>=15.0

# Stage-3 ML — A3T-GCN (plans/06). torch_geometric provides GCNConv.
torch>=2.2
torch_geometric>=2.5
scikit-learn>=1.3
tqdm>=4.66
# Kaggle-only experiment tracking (installed by the generated notebook, not required for tests):
# wandb>=0.17
# weave>=0.50
