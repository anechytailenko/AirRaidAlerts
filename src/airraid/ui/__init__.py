"""Frontend (Dash) for the Narrative Analyst — the Analysis tab (plans/08).

Solely Python (Dash + Plotly per plans/02 Stage 5). The page is the agent's client: a chat on the
right, the agent's plot on the left. Pure render helpers (`render.py`) keep the data-flow rules
testable without a browser.
"""
